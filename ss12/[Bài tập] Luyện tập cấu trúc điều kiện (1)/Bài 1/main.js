// Bài 1: Nhập vào hai số a và b, và kiểm tra xem a có chia hết cho b hay không
let a = Number(prompt("nhap so a"));
let b = Number(prompt("nhap so b"));
if (a % b === 0) {
  alert(`a chia het cho b`);
} else {
  alert(`a  0 chia het cho b`);
}
