// Bài 5: Tính diện tích tam giác vuông khi biết 02 cạnh kề a, b.
let a = Number(prompt(`nhap canh cạnh kề a`));
let b = Number(prompt(`nhap canh cạnh kề b`));
let s = (a * b) / 2; //Tính diện tích tam giác
document.write(`diện tích tam giác vuông: ${s}`);
